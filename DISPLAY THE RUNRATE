import matplotlib.pyplot as plt
import numpy as np
overs=np.arange(1,11) # Changed to include 10 overs
scores=[9,3,4,7,8,13,6,7,3,5]
plt.bar(overs,scores,color="green")
plt.title("INDIA SCORE OVER WISE")
plt.xlabel("OVER FROM 1 TO 10")
plt.ylabel("RUNS BASED ON OVERS")
plt.xticks(overs) # Corrected 'over' to 'overs'
plt.grid(axis='y',linestyle="--")
plt.show()
